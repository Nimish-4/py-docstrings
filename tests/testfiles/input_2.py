class xyz:
    def __init__(self, a, b):
        """Initialize `a` and `b`"""
        self.a = a
        self.b = b
    
    def calculate(self):
        """Calculate the sum of `a` and `b`"""
        return self.a + self.b